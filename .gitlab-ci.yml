image: node:16

cache:
  paths:
    - node_modules/

pages:
  script:
    - npm install -g pnpm
    - pnpm install
    - ./node_modules/.bin/next build
    - ./node_modules/.bin/next export
  artifacts:
    paths:
      - out
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
